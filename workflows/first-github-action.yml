name: Run Shell Commands
on: [push]

jobs: 
  run-shell-command:
    runs-on: ubuntu-latest
    step:
      - name: Echo a String
        run: echo "Hello Security Group!! This is our first GitHub Actions Workflow file"